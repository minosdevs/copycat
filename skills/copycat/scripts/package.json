{
  "name": "copycat-scripts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Playwright capture + pixel compare scripts for the copycat Claude Code skill",
  "scripts": {
    "setup": "npm install && npx playwright install chromium",
    "capture": "node capture.mjs",
    "compare": "node compare.mjs"
  },
  "dependencies": {
    "pixelmatch": "^6.0.0",
    "playwright": "^1.50.0",
    "pngjs": "^7.0.0"
  }
}
